{
  "name": "vue3-components",
  "version": "1.0.0",
  "description": "A Vue3 component library based on monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -C play dev",
    "build": "pnpm -r --filter=./packages/* run build",
    "clean": "pnpm -r --filter=./packages/* run clean"
  },
  "keywords": [
    "vue3",
    "component-library",
    "monorepo"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vue": "^3.3.0",
    "vue-tsc": "^1.8.0"
  }
}

