import { withInstall } from '@vue3-components/utils'
import Collapse from './collapse.vue'
import CollapseItem from './collapse-item.vue'

export const VcCollapse = withInstall(Collapse)
export const VcCollapseItem = withInstall(CollapseItem)

export default VcCollapse

export * from './types'

